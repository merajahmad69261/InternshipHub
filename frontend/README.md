# Frontend

React + Vite + Tailwind frontend for the AI-powered internship recommendation engine.

## Run

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env`
3. Start the app:
   `npm run dev`

The frontend expects the FastAPI backend at `http://localhost:8000/api`.
